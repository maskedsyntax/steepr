# Steepr Website

Marketing website for Steepr, built separately from the Swift app with Vue and Vite.

## Commands

```sh
npm install
npm run dev
npm run build
```

The build outputs static files to `dist/`.
